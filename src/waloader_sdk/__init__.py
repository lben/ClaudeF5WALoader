"""WALoader SDK — helpers importable by deployed child apps.

Child apps receive this package on PYTHONPATH plus WALOADER_* environment
variables injected by the WALoader process manager at launch.
"""

__version__ = "0.1.0"
