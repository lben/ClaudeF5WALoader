"""WALoader UI entrypoint (placeholder — replaced in phase P9).

Documented stub per AGENTS.md: serve (P8) needs a launchable entrypoint; the
real dashboard lands in P9 and replaces this file.
"""

import streamlit as st

st.set_page_config(page_title="WALoader", layout="wide")
st.title("WALoader")
st.info("The WALoader dashboard is being built (phase P9).")
